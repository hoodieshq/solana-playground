# Internal working documents

Temporary. These are the original briefs we were handed on 2026-08-19, kept
here so both sessions can reference them verbatim. To be removed from the
repository before the branch is merged.

- `brief/CLAUDE.original.md` — the CLAUDE.md as received; the root
  `CLAUDE.md` has since been rewritten and is the live version.
- `brief/task-00-codebase-map.md` — the codebase-map task (done:
  `docs/codebase-map.yaml`).
- `brief/task-01-assistant-panel.md` — the assistant-panel task (in progress).
- The original `product-brief.md` was identical to `docs/product-brief.md`, so
  it is not duplicated here.
