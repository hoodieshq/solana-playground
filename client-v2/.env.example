# GitHub OAuth App credentials, read by api/github-oauth.mjs.
# No REACT_APP_ prefix: that is what keeps the secret out of the browser bundle.
# Copy to .env.local for local dev; set the same names in the Vercel dashboard.
GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=
